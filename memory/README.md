# Legacy memory (mirror) — canonical is TSA

⚠️ Canonical project memory now lives in **`~/Documents/Claude/Projects/TSA/MEMORY.md`** (+ `TSA/memory/`). Per `TSA/standards/content-operating-system.md`, that is the single source of truth (status: `CANONICAL`).

These files are a **legacy mirror (`DERIVED`)** — do not edit them; update the canonical instead. They are retained only because `CLAUDE.md` and some drafts still link into this folder (e.g. `memory/projects/foundational-layer-thesis.md`, `memory/context/bitcoin-adviser.md`). A later pass will migrate those referenced files into TSA, update the pointers, then remove this folder.
